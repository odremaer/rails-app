<tr>
  <td><%= question.body %></td>
  <td><%= link_to t('admin.show'), admin_question_path(question) %></td>
  <td><%= link_to t('admin.edit'), edit_admin_question_path(question) %></td>
  <td><%= link_to t('admin.delete'),
                  admin_question_path(question),
                  method: "delete",
                  data: { confirm: t('admin.confirm') } %>
  </td>
</tr>
