<tr>
  <td><%= image_tag("badges/#{badge.image_title}", height: '32', width: '40') %><%= badge.title %></td>
  <%= create_rule(badge) %>
  <td><%= link_to t('badges.show'), admin_badge_path(badge) %></td>
  <td><%= link_to t('badges.edit'), edit_admin_badge_path(badge) %></td>
  <td><%= link_to t('badges.delete'),
                  admin_badge_path(badge),
                  method: :delete %></td>
</tr>
