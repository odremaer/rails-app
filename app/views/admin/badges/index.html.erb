<h1><%= t('badges.all') %></h1>

<table class="table">
  <tr>
    <th><%= t('badges.existed') %></th>
    <th><%= t('badges.how_to_achieve') %></th>
    <th colspan="3"><%= t('action') %></th>
  </tr>

  <%= render @badges %>
</table>

<%= link_to t('badges.new'), new_admin_badge_path %>
<p>
  <%= link_to t('all_tests'), admin_tests_path %>
</p>
